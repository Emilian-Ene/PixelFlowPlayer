<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    tools:context=".player.MainActivity">

    <!-- Group for Pairing & Paired States -->
    <LinearLayout
        android:id="@+id/pairing_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="16dp">
        <TextView
            android:id="@+id/main_status_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-thin"
            android:textColor="@color/teal_text"
            android:textSize="80sp"
            android:textStyle="bold"
            tools:text="L2DU9R" />
        <TextView
            android:id="@+id/url_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="120dp"
            android:text="@string/pairing_url"
            android:textColor="@android:color/darker_gray"
            android:textSize="22sp" />
        <TextView
            android:id="@+id/instructions_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/pairing_instructions_unpaired"
            android:textColor="@android:color/darker_gray"
            android:textSize="16sp" />
    </LinearLayout>

    <!-- Loading Bar -->
    <ProgressBar
        android:id="@+id/loading_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:indeterminateTint="@android:color/white"
        android:visibility="gone"
        tools:visibility="visible" />

    <!-- Exit Button -->
    <ImageView
        android:id="@+id/exit_button"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/exit_button_description"
        android:padding="8dp"
        android:src="@drawable/ic_close_with_background"
        android:visibility="visible"
        tools:visibility="visible" />

    <!-- Player Container -->
    <FrameLayout
        android:id="@+id/player_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">
        <androidx.media3.ui.PlayerView
            android:id="@+id/video_player_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/black"
            app:use_controller="false"
            app:resize_mode="fit"
            app:surface_type="texture_view"
            app:shutter_background_color="@android:color/black" />
        <ImageView
            android:id="@+id/image_player_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/black"
            android:scaleType="fitCenter"
            android:contentDescription="@string/image_player_description" />
    </FrameLayout>

    <!-- Offline Indicator -->
    <ImageView
        android:id="@+id/offline_indicator"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_gravity="bottom|start"
        android:layout_margin="16dp"
        android:contentDescription="@string/offline_indicator_description"
        android:src="@drawable/ic_offline_indicator_with_background"
        android:visibility="visible"
        tools:visibility="visible" />

    <!-- Download Progress View -->
    <LinearLayout
        android:id="@+id/download_progress_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:background="@android:color/black"
        android:orientation="vertical"
        android:padding="24dp"
        android:visibility="gone"
        tools:visibility="visible">

        <TextView
            android:id="@+id/download_main_status_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="16dp"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            tools:text="Downloading playlist..." />

        <ProgressBar
            android:id="@+id/download_overall_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:backgroundTint="@android:color/darker_gray"
            android:max="100"
            android:progress="30"
            android:progressTint="@color/teal_200" />

        <TextView
            android:id="@+id/download_progress_percentage_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="4dp"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            tools:text="30%" />

        <TextView
            android:id="@+id/download_items_count_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="12dp"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            tools:text="Item 2 of 5: video_presentation.mp4 (30%)" />

        <TextView
            android:id="@+id/download_error_details_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:background="#33FFFFFF"
            android:ellipsize="end"
            android:maxLines="3"
            android:padding="8dp"
            android:scrollbars="vertical"
            android:textColor="@color/dark_red_error"
            android:visibility="gone"
            tools:text="Error: video_ad.mp4 - Network timeout. Retrying...\nError: image_banner.jpg - File corrupted."
            tools:visibility="visible" />
    </LinearLayout>

</FrameLayout>
